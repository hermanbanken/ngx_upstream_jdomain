resolver 10.72.1.11;
resolver_timeout 10s;

upstream backend {
	hostname backendalias;
	#keepalive 10;
}

server {
	listen       80;
	server_name  nginx;

	location / {
		proxy_pass http://backend;
	}
}